{
  "name": "theisle-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Public player portal: Steam login, your dino, your garage, leaderboard. Talks to the bridge's read-only /player-api.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
